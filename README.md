# 🚀 O que aprendi com o projeto Gerenciador de Tarefas em Python

### 1. Estrutura de controle e loops
- Uso do `while True` para criar um programa que roda continuamente até o usuário escolher sair.
- Controle do fluxo com `if`, `elif` e `else` para decisões baseadas na entrada do usuário.

### 2. Funções
- Criação e uso de funções para organizar o código (`ver_tarefas()`, `concluir_tarefa()`, `salvar_tarefas()`, `carregar_tarefas()`).
- Passagem de dados entre funções e programa principal.

### 3. Listas
- Manipulação de listas para armazenar e gerenciar as tarefas.
- Uso de métodos como `.append()` para adicionar itens.
- Uso de `.pop()` para remover itens pelo índice.
- Percorrer listas com `for` e `enumerate()` para mostrar índices e valores.

### 4. Tratamento de erros (exceções)
- Uso do `try/except` para lidar com entradas inválidas do usuário sem travar o programa.
- Tratamento específico de erros comuns como `ValueError` e `IndexError`.

### 5. Entrada e saída de dados
- Leitura de dados do usuário via `input()`.
- Escrita e leitura de arquivos para salvar e carregar tarefas, garantindo persistência de dados entre execuções.

### 6. Manipulação de arquivos
- Abrir arquivos para leitura e escrita usando `with open(...)`.
- Garantir que o programa não quebre se o arquivo ainda não existir (`FileNotFoundError`).

### 7. Interatividade no terminal
- Criação de um menu simples e amigável para o usuário escolher ações.
- Impressão formatada no terminal usando f-strings para melhor experiência.

### 8. Boas práticas básicas
- Separação de lógica em funções para facilitar manutenção.
- Validação e feedback para o usuário, melhorando a usabilidade.
- Comentários e código organizado para facilitar entendimento.
